{
  "private": true,
  "scripts": {
    "start": "serve",
    "tailwind:build": "tailwindcss -i main.css -o dist.css --minify",
    "tailwind:watch": "tailwindcss -i main.css -o dist.css --watch"
  },
  "devDependencies": {
    "prettier": "^3.3.1",
    "prettier-plugin-tailwindcss": "^0.6.2",
    "serve": "^14.2.3",
    "tailwindcss": "^3.4.4"
  }
}
